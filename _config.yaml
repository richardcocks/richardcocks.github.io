markdown: GFM

title: Richard Cocks
description: Posts on C# / .NET performance, benchmarking, and the occasional rabbit hole.
url: https://richardcocks.github.io
baseurl: ""
author: Richard Cocks

plugins:
  - jekyll-feed
  - jekyll-redirect-from

permalink: /:year/:month/:day/:title

# Posts originally lived at the repo root and were served at /YYYY-MM-DD-Title.
# Those URLs are still linked externally, so each pre-existing post carries a
# `redirect_from` entry pointing at its old flat URL. New posts don't need one.
